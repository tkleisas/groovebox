# Build output (CMake/MSVC) — regenerable
emulator/build/
panel-fw/build/
panel-fw/build.log

# Large Raspberry Pi OS images and flash logs
os-image/*.img.xz
os-image/flash.log

# Local credentials — never commit (hash lives in userconf.txt)
os-image/customize/password.txt

# OS / editor noise
Thumbs.db
.DS_Store

# KiCad noise
*-backups/
*.kicad_pcb-bak
*.kicad_sch-bak
fp-info-cache

